const melhorGuitarrista = "Jimi Hendrix";

let a = 1;
    b = 2;
    c = 3;

let salarioMinimo = 1518;

/* As constantes possuem valores fixos, enquanto as vpariáveis (let)
                 pdoer ter seus valores alterados. */

/* O var não é recomendado, pois o var não respeita escopos de bloco,
                        apenas de funções. */

/* é bem melhor utilizar "null" quando quiserrepresentar um valor nulo,
        evitando assim ambiguidade e inconsistencias no código. */